{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"pycounts","text":"<p>Calculate word counts in a text file!</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install pycounts\n</code></pre>"},{"location":"#usage","title":"Usage","text":"<p><code>pycounts</code> can be used to count words in a text file and plot results as follows:</p> <pre><code>from pycounts.pycounts import count_words\nfrom pycounts.plotting import plot_words\nimport matplotlib.pyplot as plt\n\nfile_path = \"test.txt\"  # path to your file\ncounts = count_words(file_path)\nfig = plot_words(counts, n=10)\nplt.show()\n</code></pre>"},{"location":"api/","title":"API Reference","text":""},{"location":"api/#pycountsuv.plotting","title":"<code>plotting</code>","text":""},{"location":"api/#pycountsuv.plotting.plot_words","title":"<code>plot_words(word_counts, n=10)</code>","text":"<p>Plot a bar chart of word counts.</p> <p>Parameters:</p> Name Type Description Default <code>word_counts</code> <code>Counter</code> <p>Counter object of word counts.</p> required <code>n</code> <code>int</code> <p>Plot the top n words. By default, 10.</p> <code>10</code> <p>Returns:</p> Type Description <code>BarContainer</code> <p>Bar chart of word counts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from pycounts.pycounts import count_words\n&gt;&gt;&gt; from pycounts.plotting import plot_words\n&gt;&gt;&gt; counts = count_words(\"text.txt\")\n&gt;&gt;&gt; plot_words(counts)\n</code></pre> Source code in <code>src/pycountsuv/plotting.py</code> <pre><code>def plot_words(word_counts: Counter, n: int = 10):\n    \"\"\"Plot a bar chart of word counts.\n\n    Parameters\n    ----------\n    word_counts : collections.Counter\n        Counter object of word counts.\n    n : int, optional\n        Plot the top n words. By default, 10.\n\n    Returns\n    -------\n    matplotlib.container.BarContainer\n        Bar chart of word counts.\n\n    Examples\n    --------\n    &gt;&gt;&gt; from pycounts.pycounts import count_words\n    &gt;&gt;&gt; from pycounts.plotting import plot_words\n    &gt;&gt;&gt; counts = count_words(\"text.txt\")\n    &gt;&gt;&gt; plot_words(counts)\n    \"\"\"\n    top_n_words = word_counts.most_common(n)\n    word, count = zip(*top_n_words)\n    fig = plt.bar(range(n), count)\n    plt.xticks(range(n), labels=word, rotation=45)\n    plt.xlabel(\"Word\")\n    plt.ylabel(\"Count\")\n    return fig\n</code></pre>"},{"location":"api/#pycountsuv.pycountsuv","title":"<code>pycountsuv</code>","text":""},{"location":"api/#pycountsuv.pycountsuv.clean_text","title":"<code>clean_text(text)</code>","text":"<p>Lowercase and remove punctuation from a string.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Text to clean.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Cleaned text.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; clean_text(\"Early optimization is the root of all evil!\")\n'early optimization is the root of all evil'\n</code></pre> Source code in <code>src/pycountsuv/pycountsuv.py</code> <pre><code>def clean_text(text: str):\n    \"\"\"Lowercase and remove punctuation from a string.\n\n    Parameters\n    ----------\n    text : str\n        Text to clean.\n\n    Returns\n    -------\n    str\n        Cleaned text.\n\n    Examples\n    --------\n    &gt;&gt;&gt; clean_text(\"Early optimization is the root of all evil!\")\n    'early optimization is the root of all evil'\n    \"\"\"\n    text = text.lower()\n    for p in punctuation:\n        text = text.replace(p, \"\")\n    return text\n</code></pre>"},{"location":"api/#pycountsuv.pycountsuv.count_words","title":"<code>count_words(input_file)</code>","text":"<p>Count words in a text file.</p> <p>Words are made lowercase and punctuation is removed before counting.</p> <p>Parameters:</p> Name Type Description Default <code>input_file</code> <code>str</code> <p>Path to text file.</p> required <p>Returns:</p> Type Description <code>Counter</code> <p>dict-like object where keys are words and values are counts.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; count_words(\"text.txt\")\n</code></pre> Source code in <code>src/pycountsuv/pycountsuv.py</code> <pre><code>def count_words(input_file: str):\n    \"\"\"Count words in a text file.\n\n    Words are made lowercase and punctuation is removed\n    before counting.\n\n    Parameters\n    ----------\n    input_file : str\n        Path to text file.\n\n    Returns\n    -------\n    collections.Counter\n        dict-like object where keys are words and values are counts.\n\n    Examples\n    --------\n    &gt;&gt;&gt; count_words(\"text.txt\")\n    \"\"\"\n    text = load_text(input_file)\n    text = clean_text(text)\n    words = text.split()\n    return Counter(words)\n</code></pre>"},{"location":"api/#pycountsuv.pycountsuv.load_text","title":"<code>load_text(input_file)</code>","text":"<p>Load text from a text file and return as a string.</p> <p>Parameters:</p> Name Type Description Default <code>input_file</code> <code>str</code> <p>Path to text file.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Text file contents.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; load_text(\"text.txt\")\n</code></pre> Source code in <code>src/pycountsuv/pycountsuv.py</code> <pre><code>def load_text(input_file: str):\n    \"\"\"Load text from a text file and return as a string.\n\n    Parameters\n    ----------\n    input_file : str\n        Path to text file.\n\n    Returns\n    -------\n    str\n        Text file contents.\n\n    Examples\n    --------\n    &gt;&gt;&gt; load_text(\"text.txt\")\n    \"\"\"\n    with open(input_file, \"r\") as file:\n        text = file.read()\n    return text\n</code></pre>"},{"location":"example/","title":"Usage","text":"In\u00a0[1]: Copied! <pre>from pycountsuv.pycountsuv import count_words\n\nquote = \"\"\"Insanity is doing the same thing \nover and over and expecting different results.\"\"\"\nwith open(\"einstein.txt\", \"w\") as file:\n    file.write(quote)\ncounts = count_words(\"einstein.txt\")\nprint(counts)\n</pre> from pycountsuv.pycountsuv import count_words  quote = \"\"\"Insanity is doing the same thing  over and over and expecting different results.\"\"\" with open(\"einstein.txt\", \"w\") as file:     file.write(quote) counts = count_words(\"einstein.txt\") print(counts) <pre>Counter({'over': 2, 'and': 2, 'insanity': 1, 'is': 1, 'doing': 1, 'the': 1, 'same': 1, 'thing': 1, 'expecting': 1, 'different': 1, 'results': 1})\n</pre>"}]}